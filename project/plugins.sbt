/**
 * Starting resolvers declaration section
 * Example: resolvers += Resolver.url("Repo", url("http://repo.com"))(Resolver.ivyStylePatterns).withAllowInsecureProtocol(true)
 */
// TODO: Add any necessary resolvers
/**
 * Ending resolvers declaration section
 */

addSbtPlugin("com.twitter" % "scrooge-sbt-plugin" % "21.9.0")
